{% from "ui/icons.njk" import check %}

<section class="equipment-banner">
  <div class="container equipment-banner__inner">
    <span class="equipment-banner__icon" aria-hidden="true">{{ check() }}</span>
    <p class="equipment-banner__text">{{ equipment.banner }}</p>
  </div>
</section>
