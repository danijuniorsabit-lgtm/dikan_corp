{% from "ui/button.njk" import button %}
{% from "ui/icons.njk" import emblem %}

<section class="cta-banner">
  <div class="container cta-banner__inner">
    <div class="cta-banner__brand">
      <span class="cta-banner__mark" aria-hidden="true">{{ emblem() }}</span>
      <p class="cta-banner__text">Мы являемся лидером в своей области</p>
    </div>
    {{ button('О компании', { href: '/about.html', icon: false }) }}
  </div>
</section>
